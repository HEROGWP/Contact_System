<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to("青蟲關懷系統", teams_path, class: "navbar-brand") %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="navbar-collapse collapse" id="bs-example-navbar-collapse-1" aria-expanded="false" style="height: 1px;">
      <ul class="nav navbar-nav navbar-right">
        <% if !current_user %>
          <li> <%= link_to("註冊", new_user_registration_path, class: "glyphicon glyphicon-user") %> </li>
          <li> <%= link_to("登入", new_user_session_path, class: "glyphicon glyphicon-log-in") %> </li>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
              Hi, <%= current_user.name%>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li> <%= link_to(" 修改帳戶", edit_user_registration_path, class: "glyphicon glyphicon-cog" )%></li>
              <li> <%= link_to(" 通訊錄",contacts_path, class: "glyphicon glyphicon-list-alt" )%></li>
              <li> <%= link_to(" 點名表", teams_path, class: "glyphicon glyphicon-check" )%></li>
              <li> <%= link_to(" 壽星查詢", birthday_path, class: "glyphicon glyphicon-search")%></li>
              <li class="divider"></li>
              <li> <%= link_to(" 登出", destroy_user_session_path, method: :delete, class: "glyphicon glyphicon-log-out")%> </li>
            </ul>
          </li>
        <% end %>  
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>